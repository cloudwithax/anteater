#!/bin/bash

# Shared command list for help text and completions.
ANTEATER_COMMANDS=(
    "clean:Free up disk space from user caches"
    "purge:Remove old project artifacts"
    "analyze:Browse disk usage by directory"
    "status:Snapshot of system health"
    "optimize:Run system maintenance tasks"
    "completion:Setup shell tab completion"
    "update:Update to latest version"
    "remove:Remove Anteater from system"
    "help:Show help"
    "version:Show version"
)
